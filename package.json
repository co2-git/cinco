{
  "name": "cinco",
  "version": "0.1.0",
  "description": "cinco ===",
  "main": "index.js",
  "scripts": {
    "test": "node test",
    "build": "babel lib/ --modules common --stage 1 --out-dir dist/lib; babel index.js --modules common --stage 1 --out-file dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel": "^5.4.3"
  },
  "devDependencies": {
    "babelify": "^6.1.1",
    "express": "^4.12.3",
    "should": "^6.0.1"
  },
  "browserify": {
    "transform": [
      [
        "babelify"
      ]
    ]
  }
}
